package com.example.modelgateway.api.enums;

public enum GatewayErrorCode {
    INVALID_REQUEST,
    UNAUTHORIZED,
    QUOTA_EXCEEDED,
    MODEL_NOT_FOUND,
    PROVIDER_NOT_FOUND,
    PROVIDER_UNAVAILABLE,
    PROVIDER_TIMEOUT,
    ROUTE_NOT_FOUND,
    INTERNAL_ERROR
}
